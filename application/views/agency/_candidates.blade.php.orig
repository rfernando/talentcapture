<div class="box box-warning">
    <div class="box-header with-border">
        <h3 class="box-title"><i class="fa fa-users"></i> My Candidates</h3>
        <div class="box-tools">
            @if(!$job->closed)
                @if(!isset($subscription['type']))
                <a href="{{ base_url('searches/add_candidate/'.$job->id) }}" type="button" class="btn btn-box-tool" data-toggle="tooltip" title="Add New Candidate" >
                    <i class="fa fa-user-plus"></i>
                </a>
                @endif
            @else
                <label class="label label-danger">Job Closed</label>
            @endif
        </div>
    </div>
    <!-- /.box-header -->

    <div class="box-body {{ isset($candidates) && count($candidates) ? 'no-padding' : '' }}">
        @if(isset($candidates) && count($candidates))
            <ul class="nav nav-pills nav-stacked">
                @foreach($candidates as $j)
                    <li {{ (isset($candidate->id) && $candidate->id==$j->id) ? 'class="active"': '' }}>
                        <a href="{{ base_url('searches/candidate_detail/'.$job->id.'/'.$j->id);  }}">
                            <i class="fa fa-user"></i> {{ $j->name }}
                            {{--@if(isset($hasHireDetails) && $hasHireDetails)
                                <i class="fa fa-check-circle pull-right text-success" data-toggle="tooltip" title="Candidate Hired"></i>--}}
                            @if(isset($j->hired) && $j->hired)
                                <i class="fa fa-check-circle pull-right text-success"  data-toggle="tooltip" title="Candidate Hired"></i>
                            @elseif(isset($j->hasHireDetails) && $j->hasHireDetails)
                                <i class="fa fa-check pull-right text-success"  data-toggle="tooltip" title="Reported Hired"></i>
                            @elseif($j->hired)
                                <i class="fa fa-check-circle pull-right text-success" data-toggle="tooltip" title="Candidate Hired"></i>
                            @elseif($job->closed)
                                <i class="fa fa-circle-o pull-right" style="color: #dd4b39" data-toggle="tooltip" title="Candidate Not Selected"></i>
                            @elseif($j->client_accepted)
                                <i class="fa fa-{{ ($j->client_accepted == 1) ? 'check text-success' : 'times-circle text-danger' }} pull-right" data-toggle="tooltip" title="Candidate {{ ($j->client_accepted == 1 ) ? 'Accepted' : 'Rejected' }}"></i>
                            @else
                                <i class="fa fa-exclamation-circle pull-right text-warning" data-toggle="tooltip" title="Pending Approval"></i>
                            @endif
                        </a>
                    </li>
                @endforeach
            </ul>
        @else
            <p class="text-info">You have not added any candidate to this Job Yet. @if(!isset($subscription['type']))<a href="{{  base_url('searches/add_candidate/'.$job->id) }}">Click Here</a>  to add a new candidate to this Job @endif</p>
        @endif
    </div>
</div>
<!-- /. box -->